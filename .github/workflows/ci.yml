name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends gnome-shell gettext libglib2.0-bin

      - name: Check JavaScript syntax
        run: |
          status=0
          for f in src/*.js; do
            echo "Checking $f"
            node --input-type=module --check < "$f" || status=1
          done
          exit $status

      - name: Validate GSettings schema
        run: glib-compile-schemas --strict --dry-run src/schemas

      - name: Compile translations
        run: |
          for po in po/*.po; do
            msgfmt --output-file=/dev/null "$po"
            echo "OK: $po"
          done

      - name: Pack extension
        run: ./install.sh zip

      - name: Upload extension zip
        uses: actions/upload-artifact@v6
        with:
          name: gnockoff-tiles@episode6.com.shell-extension
          path: gnockoff-tiles@episode6.com.shell-extension.zip
